# WebSocket Configuration for Development

# Broadcasting
BROADCAST_DRIVER=pusher

# Soketi Configuration (локальный WebSocket сервер)
PUSHER_APP_ID=buildlify-app
PUSHER_APP_KEY=buildlify-key
PUSHER_APP_SECRET=buildlify-secret
PUSHER_HOST=127.0.0.1
PUSHER_PORT=6001
PUSHER_SCHEME=http
PUSHER_APP_CLUSTER=mt1

# Queue Configuration (для WebSocket событий)
QUEUE_CONNECTION=database

# Для мобильного приложения
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
