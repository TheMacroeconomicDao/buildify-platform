post:
  tags:
    - user
  description: API отправки кода для смены пароля на email
  operationId: api-user-change-password-send-code
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/User/ChangePasswordSendCode/Request.yaml
    required: true
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/User/ChangePasswordSendCode/Response.yaml
    40X:
      description: ERROR
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Error/Response.yaml
    50X:
      description: ERROR
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Error/Response.yaml
