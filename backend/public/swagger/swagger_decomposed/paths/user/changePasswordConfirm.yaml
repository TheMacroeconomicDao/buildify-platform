post:
  tags:
    - user
  description: API подтверждения смены пароля с кодом
  operationId: api-user-change-password-confirm
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/User/ChangePasswordConfirm/Request.yaml
    required: true
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/User/ChangePasswordConfirm/Response.yaml
    40X:
      description: ERROR
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Error/Response.yaml
    50X:
      description: ERROR
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Error/Response.yaml
